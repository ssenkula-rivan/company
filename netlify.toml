# This project is now deployed on Vercel
# Netlify configuration kept for reference only

[build]
  command = "npm ci && npm run build"
  publish = ".next"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
